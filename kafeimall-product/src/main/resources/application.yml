mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1 #逻辑已删除值（默认为1）
      logic-not-delete-value: 0 #逻辑未删除值（默认为0）
spring:
  datasource:
    username: root
    password: Begin123456.
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://1.12.65.107:3306/mall_pms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  application:
    name: mall-product